# Core
python-dotenv==1.0.1          #
requests==2.31.0               

# LLM / NLP
transformers           # GPT-Neo & HuggingFace pipelines
torch                  # backend for transformers
spacy                   # NER for tag extraction

# search tool
duckduckgo_search

# Optional Image Generation
diffusers
accelerate            # for running diffusers efficiently
torchvision            # for image processing

# Optional Web UI
streamlit              # for simple front-end / human-in-the-loop interaction
flask 
# Evaluation / Metrics
nltk                    # BLEU / ROUGE calculations
scikit-learn                   # Precision, Recall metrics

# Orchestration (Optional)
pydantic                # for structured messaging between agents
fastapi                # if we want to expose API endpoints
uvicorn                # for running FastAPI server
